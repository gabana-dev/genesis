# src/

**Intentionally empty.**

No cognitive architecture is implemented yet, by rule. Code is a commitment — it ossifies
whatever assumptions it encodes — so `src/` stays empty until the foundations in
[`../docs/`](../docs/) and [`../research/`](../research/) are understood well enough to
survive being made concrete.

The first file here will be earned, not started. See the one rule in the root
[`README`](../README.md) and Phase 0 in the [`roadmap`](../docs/roadmap.md).
