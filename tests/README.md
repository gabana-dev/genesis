# tests/

**Intentionally empty.**

Validation of the implementation lives here. Since `src/` is empty by design, so is this.
When the first implementation is earned, the way we test it — what would count as it
working — is decided alongside it, not bolted on after.

See the one rule in the root [`README`](../README.md).
