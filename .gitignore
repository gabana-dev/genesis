__pycache__/
*.py[cod]
.venv/
.DS_Store

# Development-period run logs. The per-origin result records in rdb_data/results/ are the
# artifact; stdout is not.
rdb_data/*.log

# The holdout is not downloaded and must never arrive here by accident. If the design is
# ever frozen and the holdout opened, this entry is removed deliberately, not silently.
rdb_data/raw_holdout/

# Derived: regenerate with `.venv/bin/python dashboard.py`. A snapshot is not evidence.
dashboard.html

# Commercial material stays out of the public repo. The RESEARCH is public on purpose --
# that is the distribution strategy. Pricing, competitive analysis and internal odds are not.
BUSINESS-PLAN.md
/private/

# Astro / node
web/node_modules/
web/.astro/
web/dist/
